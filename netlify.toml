# Publish the whole `site/` folder as the site root.
[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  # Node 18+ provides global fetch, which the conflict proxy uses.
  node_bundler = "esbuild"
